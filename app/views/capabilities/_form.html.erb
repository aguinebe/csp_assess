<%= form_with(model: capability, local: true) do |form| %>
  <% if capability.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(capability.errors.count, "error") %> prohibited this capability from being saved:</h2>

      <ul>
      <% capability.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :level %>
    <%= form.number_field :level, id: :capability_level %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :capability_description %>
  </div>

  <div class="field">
    <%= form.label :recommendation %>
    <%= form.text_field :recommendation, id: :capability_recommendation %>
  </div>

  <div class="field">
    <%= form.label :subcategory_id %>
    <%= form.number_field :subcategory_id, id: :capability_subcategory_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
